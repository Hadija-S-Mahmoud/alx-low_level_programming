#include <stdio.h>
/**
* main - prints all arguments it receives
* @argc: argument count
* @argv: argument
* Return: 0
*/
int main(int argc, char **argv)
{
int j;
for (j = 0; j < argc; j++)
printf("%\n", argv[i]);
return (0);
}
